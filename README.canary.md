# buildgpt-canary-green

Minimal always-green repo for CodeOps merge canary tests.

## Setup

To set up the repository, simply clone it and ensure you have the necessary permissions to run GitHub Actions. No additional configuration is required.
